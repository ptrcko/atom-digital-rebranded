    source "https://rubygems.org"

    # Use GitHub Pages gem — ensures the same Jekyll/plugins as GitHub Pages
    # gem "github-pages", group: :jekyll_plugins

    # Optional: Add Jekyll SEO plugin (already included in github-pages)
    # gem "jekyll-seo-tag"

    # Optional: Add Jekyll Sitemap (already included in github-pages)
    # gem "jekyll-sitemap"

    # Optional: Add Jekyll Feed (already included in github-pages)
    # gem "jekyll-feed"

    # If using Windows, enable these:
    # gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]

    # Ensure Bundler uses the correct Ruby version

    gem "jekyll", "~> 4.3"
    gem "webrick"
    gem "jekyll-postcss"
    
    gem "jekyll-seo-tag"
    gem "jekyll-sitemap"
    gem "jekyll-feed"
    gem "jekyll-include-cache"

    ruby RUBY_VERSION
